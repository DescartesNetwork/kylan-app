export enum PayState {
  Mint = 'mint',
  Payback = 'payback',
}

export const CERTIFICATE_STATUS = {
  active: 'active',
  mint_only: 'mint-only',
  burn_only: 'burn-only',
  pause: 'pause',
}

export const KUSD_DECIMAL = 6
